name: Generate Podcast Feeds
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1.Checkout repo
        uses: actions/checkout@v3
      - name: 2.Run feed generator
        uses: /gsp00/podcast-generator@main
        
          
          
          
      
